<p style="color: green"><%= notice %></p>

<%= render @movie %>

<div>
  <%= link_to "Edit this movie", edit_movie_path(@movie, sort: session[:sort], direction: session[:direction]) %> |
  <%= link_to "Back to movies", movies_path(sort: session[:sort], direction: session[:direction]) %>

  <%= button_to "Destroy this movie", @movie, method: :delete, sort: session[:sort], direction: session[:direction] %>
</div>
